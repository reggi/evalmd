npm-debug.log
node_modules
.DS_Store
yarn-error.log

# build output; generated by the `prepack` script before publishing
dist

# Only apps should have lockfiles
npm-shrinkwrap.json
package-lock.json
yarn.lock
