npm-debug.log
node_modules
.DS_Store
yarn-error.log

# build output; generated by the `prepack` script before publishing
dist

# compiled tests; generated by `postbuild` before running the suite
dist-test

# Only apps should have lockfiles
npm-shrinkwrap.json
package-lock.json
yarn.lock

.nyc_output
coverage/
